spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: google-client-id
            client-secret: google-client-secret
          facebook:
            client-id: google-client-id
            client-secret: google-client-secret
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  datasource: # <- 추가
    url: jdbc:h2:tcp://localhost:8010/mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:

custom:
  oauth2:
    kakao:
      client-id: REST API KEY

logging:
  level:
    com:
      community: debug